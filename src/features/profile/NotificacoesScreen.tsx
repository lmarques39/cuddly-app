import React from 'react';
import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export function NotificacoesScreen() {
  return (
    <PlaceholderScreen
      title="Notificações"
      body="Lembretes para consultas e mamadas chegam com expo-notifications (P1). Ainda não há notificações configuráveis."
    />
  );
}
