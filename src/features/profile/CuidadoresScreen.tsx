import React from 'react';
import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export function CuidadoresScreen() {
  return (
    <PlaceholderScreen
      title="Cuidadores"
      body="Partilha de registos entre cuidadores em tempo real chega com o Firebase (v1.1). Por agora, os dados ficam só neste dispositivo."
    />
  );
}
