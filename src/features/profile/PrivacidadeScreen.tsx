import React from 'react';
import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export function PrivacidadeScreen() {
  return (
    <PlaceholderScreen
      title="Privacidade e dados"
      body="Exportação e eliminação de conta chegam com a conta na cloud (Firebase, tarefa #10). Por agora, todos os dados desta demo ficam só neste dispositivo e podem ser apagados desinstalando a app."
    />
  );
}
